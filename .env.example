# Password condivisa per entrare nel sito (schermata di login).
SITE_PASSWORD=cambiami

# Stringa di connessione al database Postgres (es. Vercel Postgres / Neon / Supabase).
# Esempio: postgresql://user:password@host:5432/dbname?sslmode=require
DATABASE_URL=
